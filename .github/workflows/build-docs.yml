name: Generate documentation

on:
  workflow_call:

jobs:
  run-typedoc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Use Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
      - run: npm ci
      - run: npm run docs

